# Ignora arquivos de log gerais
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Ignora diretórios de dependências e builds
node_modules/    # Pacotes npm instalados
dist/            # Arquivos de build de produção
dist-ssr/        # Arquivos de build para server-side rendering
*.local          # Configurações locais geradas

# Ignora arquivos e pastas de ambiente de editor e sistema
.vscode/         # Configurações do VSCode
!.vscode/extensions.json  # Exceto o arquivo que lista extensões recomendadas
.idea/           # Configurações do JetBrains IDEs
.DS_Store        # Arquivo oculto do macOS
*.suo            # Arquivos de solução do Visual Studio
*.ntvs*          # Arquivos de configuração do Node Tools for VS
*.njsproj        # Arquivos de projeto do Node.js no VS
*.sln            # Arquivos de solução do Visual Studio
*.sw?            # Arquivos temporários de editores (vim/sw?)

# Ignora arquivo de variáveis de ambiente
.env             # Arquivo com variáveis sensíveis de ambiente
