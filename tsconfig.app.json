{
  "compilerOptions": {
    // Define o alvo do JavaScript para ES2020, garantindo compatibilidade com recursos modernos
    "target": "ES2020",

    // Usa define para campos de classe, alinhando-se à especificação ECMAScript mais recente
    "useDefineForClassFields": true,

    // Inclui bibliotecas de tempo de execução ES2020, DOM e DOM.Iterable para tipagens de APIs web
    "lib": ["ES2020", "DOM", "DOM.Iterable"],

    // Gera módulos no formato ESNext para compatibilidade com bundlers modernos
    "module": "ESNext",

    // Ignora checagem de tipos em arquivos de bibliotecas externas para acelerar a compilação
    "skipLibCheck": true,

    /* Bundler mode */
    // Usa resolução de módulos otimizada para bundlers como Vite
    "moduleResolution": "bundler",
    // Permite importar arquivos TypeScript com extensão .ts diretamente
    "allowImportingTsExtensions": true,
    // Garante que cada arquivo seja tratado como módulo independente
    "isolatedModules": true,
    // Força detecção de módulos mesmo sem imports/exports explícitos
    "moduleDetection": "force",
    // Não gera arquivos de saída, usado apenas para checagem de tipos
    "noEmit": true,
    // Habilita suporte JSX usando a transformação "react-jsx" do React 17+
    "jsx": "react-jsx",

    /* Linting */
    // Ativa verificações estritas de tipagem
    "strict": true,
    // Gera erro para variáveis locais não utilizadas
    "noUnusedLocals": true,
    // Gera erro para parâmetros de função não utilizados
    "noUnusedParameters": true,
    // Gera erro em casos de switch sem break ou retorno
    "noFallthroughCasesInSwitch": true
  },
  // Inclui todos os arquivos dentro da pasta src na compilação do projeto
  "include": ["src"]
}