{
  // Lista de arquivos individuais a serem incluídos no projeto (vazio significa incluir todos por referências)
  "files": [],

  // Referências para outros arquivos de configuração (Project References)
  "references": [
    {
      // Aponta para o arquivo de configuração principal da aplicação
      "path": "./tsconfig.app.json"
    },
    {
      // Aponta para as configurações específicas de ambiente Node
      "path": "./tsconfig.node.json"
    }
  ]
}
